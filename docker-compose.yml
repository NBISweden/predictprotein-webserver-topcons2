version: '3'

services:

  topcons:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/topcons
    working_dir: /app
    command: /topcons/dockerrun.sh
    ports:
      - "8000:8000"
    network_mode: 'host'

